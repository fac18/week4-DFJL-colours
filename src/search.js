let searchResults = [];
const colorNames = require('./color-names.json');

function searchColors(inputQuery) {
  console.log(colorNames);

  return ['aqua', 'aquamarine'];
}

module.exports = searchColors;